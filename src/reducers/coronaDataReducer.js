import initialState from './initialState';

export default function coronaDataReducer(state = initialState.coronaData, action) {
  switch (action.type) {
    default: {
      return state;
    }
  }
}
